import 'package:flutter/material.dart';

/// 기본 색상 상수들을 정의한다. 현재 파일 내에 존재하는 색상 상수는 학원나우 컬러에서 가져왔다.
/// 필요에 따라 기존 색상 상수를 제거, 추가 또는 수정하여 사용한다.

/// 기본 색상
const PRIMARY_COLOR = Color.fromRGBO(54, 193, 197, 1.0); // (54, 193, 197)
const LIGHT_PRIMARY_COLOR = Color(0xffe5feff); // (229, 254, 255)
const DEEP_PRIMARY_COLOR = Color(0xFF2CBABE);

/// 기본 회색
const GREY_COLOR = Color.fromRGBO(247, 248, 250, 1.0);

/// 어두운 회색
const DARK_GREY_COLOR = Color.fromRGBO(237, 239, 242, 1.0);

const LIGHT_GREY_COLOR = Color.fromRGBO(253, 253, 253, 1.0);

const DEEP_GREY_COLOR = Color.fromRGBO(233, 235, 239, 1.0); // (233, 235, 239)

/*
  텍스트 색상
 */

const GREY_TEXT_COLOR = Color.fromRGBO(184, 188, 198, 1.0);
const LIGHT_GREY_TEXT_COLOR = Color.fromRGBO(127, 140, 167, 1.0); // (127, 140, 167)
const DARK_GREY_TEXT_COLOR = Color.fromRGBO(96, 96, 96, 1.0);
const DEEP_BLUE_GREY_TEXT_COLOR = Color.fromRGBO(71, 80, 98, 1.0); // (71, 80, 98)
const LIGHT_BLUE_GREY_TEXT_COLOR = Color.fromRGBO(52, 60, 78, 1.0);

const SEMI_BLACK_TEXT_COLOR = Color.fromRGBO(38, 38, 38, 1.0); // (38, 38, 38)
const SEMI_LIGHT_BLACK_TEXT_COLOR = Color.fromRGBO(48, 49, 51, 1.0);

const GREEN_TEXT_COLOR = Color.fromRGBO(94, 195, 136, 1.0);

/*
  위젯 계열 색상
 */
const SHADOW_COLOR = Color(0x297c7c7c); // (124, 124, 124)